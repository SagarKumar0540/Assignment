import { ThemeColors } from "../common/models/themeColorsModel";
import { lightColors,darkColors } from "./colors";

export const lightTheme: ThemeColors = Object.freeze({...lightColors})
export const darkTheme: ThemeColors = Object.freeze({...darkColors})
